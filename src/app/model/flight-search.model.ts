export class FlightSearch {
    source : string;
    destination : string;
    originDate : Date;

    constructor( source : string, destination : string, originDate : Date){
        this.source = origin;
        this.destination = destination;
        this.originDate = originDate;
    }
}
